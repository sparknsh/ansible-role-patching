deb {{ 'http://' if not patching_https else 'https://' }}{{ patching_mirror_url }}/ {{ ansible_distribution_release }}-backports main
{{ '# ' if not patching_repo_source else '' }}deb-src {{ 'http://' if not patching_https else 'https://' }}{{ patching_mirror_url }}/ {{ ansible_distribution_release }}-backports main
